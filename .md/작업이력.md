# 작업 이력

> 각 세션에서 수행한 작업을 날짜/시간순으로 기록
> 최신 작업이 상단에 위치

---

## 2026-03-31

### 리팩토링 — 중복 코드 제거 완료
- CHANNEL_LABEL_MAP, CHANNEL_COLOR_MAP, CHANNEL_DOT_MAP → constants.ts 통합
  - 적용: calendar, contents, contents/[id], contents/new, channel-write (5곳)
  - 색상 기준 통일: blog=green, threads=purple, instagram=pink, facebook=blue

### brand-voices — is_default 토글 공통화
- clearDefaultBrandVoice() 함수 생성 → brand-voices/route.ts
- 적용: brand-voices/route.ts, brand-voices/[id]/route.ts (2곳)

### 테스트 환경 구축
- Playwright Smoke Test 15개 (전체 통과)
- E2E Test: happy-path, guard, edit, quick-create, empty-state
- Mobile Test: iPhone 13, Galaxy S21
- 최종: PASSED 34개, FIXME 3개(앱 버그), FAILED 0개

### 버그 수정
- contents/page.tsx 스켈레톤 미전환: token 체크를 run() 안으로 이동
- settings toast 타이밍: locator 필터 수정 + test.fixme() 제거
- create/draft-info 제목 생성 실패:
  - errorMessage 고정 문구 제거 → 실제 API 에러 표시
  - handleApiError, rate-limit 응답에 success: false 추가
  - 원인: Anthropic API 크레딧 부족 → 충전 완료

### 환경 세팅 완료
- Git 설정: humanpivot / humanpivotmd
- gh CLI 설치 + GitHub 연동
- Figma Desktop MCP 연결
- Notion MCP 연결
- settings.json 백업 방법 확인

### 문서 생성
- DEVELOPMENT_GUIDE.md (656줄) — 비개발자용 전체 가이드
- UTILS_MAP.md — 공통 유틸 맵
- TEST_COMMANDS.md — 테스트 명령어
- CLAUDE.md 작업 지침 정립

### 문서 전수 평가 + CLAUDE.md 지침 정합성 수정
- **문서 평가**: v3(8개) + 설계문서(5개) 전수 점검 → 16개 문제 발견, 전부 수정
- **수정 파일 11개**:
  - `.env.example` — ANTHROPIC_API_KEY / CLAUDE_API_KEY 추가 (신규 배포 시 AI 기능 불가 방지)
  - `v3/05_페이지구조.md` — 누락 18개 페이지 추가 (dashboard, pricing, admin/*, login 등)
  - `v3/02_DB스키마.md` — contents type/channel 관계 명확 설명 추가
  - `v3/03_API목록.md` — include=contents 파라미터, API키 비고
  - `v3/06_보안체크리스트.md` — PATCH 허용 필드 13개 반영
  - `v3/08_향후계획.md` — industries 37중분류 완료 반영
  - `v3/01_서비스개요.md` — contents type→channel 변경 사항 반영
  - `설계문서/API목록.md` — PUT→PATCH 수정, DELETE 추가 (68→69개)
  - `설계문서/아키텍처.md` — API 69개, 마이그레이션 5개 반영
  - `CLAUDE.md` — 공통 유틸 맵 14개 항목 추가
  - `INDEX.md` — v3 vs 설계문서 용도 구분 명시, 갱신일 업데이트
- **CLAUDE.md 지침 준수 점검**: 8개 항목 전수 확인
  - `image-script/page.tsx:48` — `c.type`→`c.channel` 버그 수정 (이미지 생성 시 채널 못 읽는 문제)
  - 상수, API helpers, auth-client, FlowGuard, contents 컬럼 — 모두 정상
  - 미적용(useAsyncAction, pagination) — CLAUDE.md "나중에 할 것"에 이미 기록됨

### 프로덕션 DB ↔ 코드 스키마 불일치 일괄 수정
- **원인**: contents 테이블의 프로덕션 DB 컬럼이 마이그레이션 SQL과 완전히 다름
  - 프로덕션: `channel`, `keyword`, `metadata`, `meta`, `scheduled_date`, `published_at`, `tokens_used`
  - 마이그레이션: `type`, `keyword_id`, `hashtags`, `tone`, `word_count`, `ai_model` 등
- **projects 테이블**: `core_message` 컬럼 누락 → 마이그레이션 004 추가 + DB 적용
- **contents 테이블**: insert/select에서 존재하지 않는 컬럼 참조 → 500 에러
- 수정 파일 (9개):
  - `pipeline/route.ts` — insert 컬럼을 프로덕션 DB에 맞게 변경, 에러 로그 추가
  - `contents/route.ts` — keyword_id, hashtags, seo_score 제거
  - `projects/route.ts`, `projects/[id]/route.ts` — select에서 type→channel
  - `calendar/route.ts` — select에서 word_count 제거
  - `generate/route.ts` — insert 컬럼 정리
  - `channel-write/page.tsx`, `contents/page.tsx`, `calendar/page.tsx` — type→channel
  - `types/index.ts` — Content.type→Content.channel
- **설계문서 반영**: DB스키마.md 프로덕션 실제 스키마로 업데이트
- API 테스트: 프로젝트 생성→PATCH→초안→파이프라인 전체 통과 확인

### draft-info PATCH 응답 체크 추가
- `draft-info/page.tsx`에서 PATCH 응답 `success` 미체크 → 실패 시 무조건 페이지 이동
- 수정: 응답 체크 후 실패 시 에러 표시

### titles/pipeline 에러 상세 로그 추가
- `titles/route.ts`, `pipeline/route.ts` catch 블록에 console.error 상세 로그
- Claude API 에러 시 status, body, apiKeyPrefix 서버 로그 출력

---

## 2026-03-30

### 로딩 패턴 개선 — 병렬 API, 스켈레톤 UI, 세션캐싱
- dashboard: business-profile + usage 병렬 호출, usage 세션캐싱
- keywords: business-profile + keywords 병렬 호출 (2개 useEffect → 1개)
- contents: 프로젝트 카드형 스켈레톤, 프로젝트 목록 세션캐싱
- settings: BusinessProfileTab 병렬 호출 + 업종 캐싱, PlanTab usage 캐싱
- calendar: calendar + projects 병렬 호출
- `loading.tsx` 5개 신규 생성 (dashboard, keywords, contents, calendar, settings)

### business-profile Context로 중복 API 호출 제거
- `useBusinessProfile` 훅 + `BusinessProfileProvider` Context 신규
- layout.tsx에서 Provider 감싸기
- 7개 파일에서 개별 fetch → Context 1회로 통합
- 대상: SetupRequired, dashboard, keywords, draft-info, settings/BusinessProfileTab

### 성능 최적화
- **contents N+1 API 해결**: `include=contents` 쿼리 파라미터 추가, 개별 호출 50회 → 0회
- **useMemo 적용**: dashboard(usageCards), contents(filtered), calendar(projectGroups)
- **렌더 내부 함수 외부 이동**: getChannelStatus, getScheduledDate, getDotColor

### 토큰 처리 통일
- `sessionStorage.getItem('token')` 직접 사용 → `getToken()`/`authHeaders()` 변환
- 대상 8파일: contents/page, contents/[id], contents/new, pricing, onboarding, landing, admin/layout, dashboard/layout

### settings/page.tsx 탭별 코드 스플리팅
- 1,514줄 → 106줄 (93% 감소)
- 8개 탭 → src/components/settings/*.tsx 분리
- dynamic import + 스켈레톤 lazy loading 적용

### 시나리오 테스트 안정화
- loginTestAccount timeout 15s→30s
- 설정 저장 버튼 dynamic import 대기 추가
- AI 제목 생성 대기 60s→120s
- 전체 시나리오 timeout 300s 통일

---

## 2026-03-29

### 시나리오 테스트 키워드 등록 안정화
- registerKeyword: 서비스명 조합 토글 OFF, 대기시간 증가
- 키워드 등록 후 목록 표시 대기 15초로 통일

---

## 2026-03-28

### 키워드 등급 비교 테이블 + 영상 이미지 프롬프트 BottomSheet
- keywords/page.tsx: 등급 비교 탭 추가 (grade 키워드 최대 10개, 등급순 정렬, 접기/펼치기)
- video-script/page.tsx: 이미지 프롬프트 BottomSheet 추가 (AI도구/스타일/생성/복사)
- video-script: /api/generate/image-script API 연동

### 4개 시나리오 E2E 테스트 자동화 추가
- @faker-js/faker 설치 (가상 유저 데이터 생성)
- tests/helpers/test-user.ts: 회원가입/로그인/마이페이지설정/키워드등록/STEP3~7 헬퍼
- scenario-blog.spec.ts: 블로그만 B2C 전체 플로우 (9스텝)
- scenario-instagram.spec.ts: 인스타만 B2C 상품소개 (9스텝)
- scenario-b2b.spec.ts: B2B 전체채널 서비스소개 (9스텝)
- scenario-admin.spec.ts: 관리자 페이지 검증 (5스텝)
- playwright.config.ts: scenario 프로젝트 추가, 타임아웃 180초, retries 1

### PRD US-15 에이전시 팀 자동화 테스트 등록

### 시나리오 테스트 회원가입→기존계정 로그인으로 전환
- test-user.ts: loginTestAccount() 헬퍼 추가 (TEST_EMAIL 환경변수 사용)
- scenario-blog/instagram/b2b: registerUser→loginTestAccount로 전환
- 이메일 인증 미완료 문제 해결

### 5개 페이지 전면 개편
- **keywords/page.tsx**: 탭 3개 제거 → 단일 화면, 그룹 입력 제거, 등록→검색 버튼, 검색 시 자동 등록+등급분석, 초보자/전문가 토글 제거, 전체 등급 분석 버튼 제거, 리스트형+인라인 열림/닫힘
- **draft-info/page.tsx**: 자동 생성 제목 하단에 직접 입력 추가, 입력 시 자동 선택 해제
- **contents/page.tsx**: 채널별→키워드(프로젝트) 단위 카드, 탭 제거, 채널 상태 표시, 발행 메모 날짜, [전체 보기] 펼침
- **channel-write/page.tsx**: 컨펌 버튼 위에 발행 예정일 날짜 선택 추가 (scheduled_date 활용)
- **calendar/page.tsx**: 프로젝트 단위 표시, 회색/파랑/초록 색상 구분, 안내 문구, 날짜 변경 기능
- 테스트 셀렉터 4곳 UI 변경 반영
- PRD US-16~20 추가 (모두 통과)
- Ralph Loop: 타입체크✅ 빌드✅ 스모크 16/16✅

---

## 2026-03-26

### pagination.ts — 페이지네이션 공통화
- `src/lib/pagination.ts` 생성 — `parsePagination`, `paginatedResponse`
- 적용: keywords/route.ts, contents/route.ts, admin/users/route.ts (3곳)
- 미적용: projects/route.ts (프론트 응답 형식 변경 필요 → 별도 작업)

### api-helpers.ts — CRUD 헬퍼 공통화
- `src/lib/api-helpers.ts` 생성 — `getOwnedRecord`, `updateOwnedRecord`, `deleteOwnedRecord`
- deleteOwnedRecord 적용 6곳: keywords, contents, brand-voices, channels, schedules, projects
- updateOwnedRecord 적용 3곳: keywords, contents, schedules
- getOwnedRecord 적용 2곳: keywords, contents
- ~250줄 중복 → 11줄로 축소

### useAsyncAction.ts — Toast+Loading 패턴 공통화
- `src/hooks/useAsyncAction.ts` 생성 — `run`, `showToast`, `clearToast`
- 적용 16개 페이지 (calendar, contents, admin/*, support, create/* 등)
- SSE 파일(contents/new, channel-write)은 `showToast` 직접 호출로 처리
- 미적용: settings, keywords, login, onboarding, landing (구조 복잡)

### auth-client.ts — 클라이언트 인증 공통화
- `src/lib/auth-client.ts` 생성 — `getToken`, `authHeaders`
- 18개 파일에서 중복 제거 → 1곳에서 import

---

## 2026-03-25

### constants.ts 통합 — 하드코딩 제거
- `src/lib/constants.ts` 신규 생성 — 17개 상수 그룹 단일 출처
- 수정된 파일 8개:
  - `settings/page.tsx` — 7개 상수 제거 → import
  - `create/draft-info/page.tsx` — TOPIC_TYPES, TONE_OPTIONS, PROMPT_MODES
  - `contents/page.tsx` — CHANNEL_TABS, STATUS_MAP
  - `contents/new/page.tsx` — CHANNELS, TONES, STYLES
  - `create/video-script/page.tsx` — FORMAT_OPTIONS, CHANNEL_OPTIONS, STYLES
  - `lib/usage.ts` — ACTION_TYPE_MAP
  - `api/mypage/business-profile/route.ts` — B2B/B2C, 성별 검증
- 원칙: 채널/톤/상태/옵션 등 추가·수정 시 constants.ts 1곳만 변경

### 문서 체계 정리 — 25개 → 14개 축소
- 개발문서/ + 참조문서/ 삭제 → 설계문서/ 5개로 통합 (중복 제거)
- 서비스가이드/ 8개 유지 (코드로 알 수 없는 기획 의도)
- INDEX.md에 문서 운영 원칙 명시

### 문서 체계 구축
- `.md/` 폴더에 서비스가이드(8개), 개발문서(7개), 참조문서(10개) 총 25개 문서 생성
- INDEX.md 작성 (서비스가이드/개발문서/참조문서/v3 4개 섹션)
- 이전 프로젝트(marketing-app-v2) 문서 구조 참조하여 동일 체계 적용

### 업종별 컨텍스트 시스템 구현
- DB industries 테이블에 **중분류 37개** INSERT (14개 대분류 모두에 하위 업종 추가)
- `getIndustryContext()` 함수 구현 (`src/lib/prompts/pipeline.ts`)
  - 37개 중분류별 핵심 가치, 주요 고객, 필수 요소, 차별화 포인트, 감정 트리거, 주의사항 매핑
  - STEP3(제목), STEP4(초안), STEP5(채널 변환) 프롬프트에 자동 주입

### 마이페이지 설정 UI 개선
- 설정 > 마이페이지 탭 내 섹션 구분 추가
  - "업종 설정" 섹션 헤더 (B2B/B2C, 업종 선택, 기본 정보 등 상단)
  - "블로그 주제 설정" 섹션 헤더 (블로그 카테고리)
- 블로그 카테고리를 하단에서 **업종 선택 바로 아래**로 이동

---

<!--
## YYYY-MM-DD

### 작업 제목
- 변경 내용 요약
- 관련 파일: `경로`
- 비고: 참고사항
-->
