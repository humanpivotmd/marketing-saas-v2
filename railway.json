{
  "build": {
    "buildCommand": "npm run build && cp -r public .next/standalone/ && cp -r .next/static .next/standalone/.next/"
  },
  "deploy": {
    "startCommand": "node .next/standalone/server.js"
  }
}
